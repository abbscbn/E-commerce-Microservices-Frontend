export interface requestProduct {
  name: string;
  price: number;
  description: string;
  stock: number;
}
export interface responseProduct {
  id: number;
  name: string;
  price: number;
  description: string;
  stock: number;
}
